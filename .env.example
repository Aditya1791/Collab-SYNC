# APP_URL: The URL where this application is hosted.
# Used for self-referential links, socket callbacks, and API endpoints.
APP_URL="http://localhost:3000"

# PORT: The port the backend server listens on.
PORT=3000
